import React from 'react';
import Button from 'react-bootstrap/Button'

function NextButton() {
    
    return (
        <div>
            <Button variant="outline-light" className="NextBtn">다음 문제로</Button>
        </div>
    );
}

export default NextButton;